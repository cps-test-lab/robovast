# The browser scene descriptor the run view's 3D panel renders, compiled from the world by the
# .vast's execution.generate: block at campaign preparation. Derived, and ~11 MB of mesh buffers
# and textures -- never committed.
files/scene/
# Where `vast serve --results-dir .` lands this project's campaigns.
results/
